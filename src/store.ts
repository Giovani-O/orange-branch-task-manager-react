// import { create } from 'zustand'

// const useStore = create((set) => ({
//   jwtToken: '',
//   setJwtToken: (token: string) => set({ jwtToken: token }),
//   clearJwtToken: () => set({ jwtToken: '' }),
//   logout: () => set({ jwtToken: '' }),
//   login: (token: string) => set({ jwtToken: token }),
// }))
